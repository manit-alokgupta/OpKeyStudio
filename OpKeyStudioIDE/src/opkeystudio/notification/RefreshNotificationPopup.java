package opkeystudio.notification;

//import org.eclipse.mylyn.commons.ui.dialogs.AbstractNotificationPopup;
import org.eclipse.swt.SWT;
import org.eclipse.swt.graphics.Font;
import org.eclipse.swt.graphics.FontData;
import org.eclipse.swt.layout.GridData;
import org.eclipse.swt.layout.GridLayout;
import org.eclipse.swt.widgets.Composite;
import org.eclipse.swt.widgets.Display;
import org.eclipse.swt.widgets.Label;

//public class RefreshNotificationPopup extends AbstractNotificationPopup {

public class RefreshNotificationPopup {
	/*
	 * public RefreshNotificationPopup(Display display) { super(display); }
	 * 
	 * protected void createContentArea(Composite parent) {
	 * 
	 * Composite container = new Composite(parent, SWT.NULL);
	 * 
	 * GridData data = new GridData(SWT.FILL, SWT.CENTER, true, false, 1, 1);
	 * container.setLayoutData(data);
	 * 
	 * container.setLayout(new GridLayout(1, false));
	 * 
	 * Label successMsg = new Label(container, SWT.NULL);
	 * successMsg.setText("Refresh Successfully"); FontData fontData =
	 * successMsg.getFont().getFontData()[0]; Font font = new
	 * Font(Display.getCurrent(), new FontData(fontData.getName(),
	 * fontData.getHeight(), SWT.BOLD | SWT.CENTER)); successMsg.setFont(font);
	 * GridData message = new GridData(); message.widthHint = 400;
	 * successMsg.setLayoutData(message); }
	 * 
	 * protected String getPopupShellTitle() { return "OpKey"; }
	 */
}
